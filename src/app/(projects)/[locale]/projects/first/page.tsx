import React from 'react';

import { PageProps } from '@/types/globals';

export default async function HomeProject({}: PageProps) {
  return (
    <main>
      <h1>Welcome. Everything is fine !</h1>
    </main>
  );
}
